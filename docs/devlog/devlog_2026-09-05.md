# 开发日志：2026-09-05

## 今日资源同步

- [x] 拉取远端最新资源，当前分支从 `956d194cb` 快进至 `d8f5f077c`，未出现合并冲突。
- [x] 本次同步包含 SilverMind 的 `cut-fill` 构建文件、预览页与 Service Worker 更新，以及 AIChat `spaces.js` 调整；不计作本轮 MagicHaqi 功能实现。

## 昨日进度延续

- 昨日已完成新手远征短路线布局、今日任务面板层级与拖动修复、宠物捕捉公式、永久捕捉历史和图鉴再次孵化规则。
- 昨日自动化验证结果为 MagicHaqi 全量 Node 测试 `299 / 299`、小游戏目录 Playwright 冒烟测试 `57 / 57` 通过。
- 第二次两章远征、成年伙伴小镇活动链路、跨端关键页面回归，以及捕捉后再次孵化的真实 UI 链路尚未完成，今天继续处理。

## 今日计划

- [ ] 补齐两章远征的真实 UI 验收，连续检查准备、路线、战斗、奖励、退出、历史记录和成果落点；当前已首次打通第一章并进入第二章。
- [ ] 使用具备成年伙伴的真实测试账号，验证“小镇活动 -> 奖励 -> 伙伴照料 / 冒险准备”完整链路；账号条件不足时记录阻塞，不修改存档或年龄数据绕过准入。
- [ ] 回归伙伴、小镇、远征、矿区和结算之间的入口与返回路径，处理需要猜测、重复点击或状态反馈不清的问题。
- [ ] 在桌面、手机和 Pad 上复测今日任务拖动、缩放、层级切换和任务跳转，并检查关键页面是否存在横向溢出、按钮遮挡、图片破损或返回路径中断。
- [ ] 真实走通“捕捉宠物 -> 图鉴解锁 -> 宠物不在当前列表 -> 100 金币 N 品质再次孵化”，并确认宠物放归后永久捕捉历史仍有效。
- [ ] 完成改动与验收后，重新运行 MagicHaqi 全量 Node 测试和小游戏目录 Playwright 冒烟测试，记录最终结果。

## 后续评审事项

- [ ] 宠物夜行订单实施前，评审掉盒概率、免费星钥来源、低龄随机付费合规、订单异常恢复和小游戏宿主可信边界。
- [ ] HelloLearner 接入先完成只读适配方案与奖励合同评审，保持 `webgames/tools/HelloLearner/` 零修改。
- [ ] 整理负责人汇报所需立项材料：目标用户、家长付费理由、微信支付路径、版号与未成年人合规边界、验证阶段成本和停止条件。

## 执行约束

- 真实 UI 验收与自动化测试分开记录，自动化测试结果不能代替完整玩家链路。
- 远征继续使用游客真实档案和公开 UI 操作，不修改存档、年龄、体力或战斗数值，不跳层。
- 成年伙伴相关验证必须使用满足年龄条件的真实账号。
- 宠物夜行订单和 HelloLearner 奖励接入目前仍处于方案评审阶段，未经评审不计入已实现功能。

## 今日进展

- [x] 通过新的前台浏览器会话真实进入游客模式并恢复本地档案；旧共享标签页因处于后台，自动化指针和键盘事件虽命中控件但未被页面处理，新前台会话点击正常，确认不是登录页产品故障。
- [x] 核对当前真实游客条件：档案只有“幼崽 #AAAA”，初始体力 `0`、金币 `100`，仍处于新手航线起点；通过公开伙伴照料 UI 使用无限“原始饲料”合法恢复体力，未修改存档、年龄、体力或战斗数值绕过准入。
- [x] 完成今日任务面板真实 UI 回归：在当前窄屏会话中轻微拖动后面板仍完整位于 Field 舞台，页面 `scrollWidth = clientWidth`，未复现拖动后消失或横向溢出。
- [x] 完成手机 `390×844`、Pad `1024×768`、桌面 `1366×768` 三档展开态检查；任务卡均完整位于 `#mhStage` 内，三档横向溢出和破损图片均为 0。
- [x] 任务跳转与返回闭环通过：从任务卡进入宠物图鉴时面板隐藏，使用图鉴返回按钮回到 Field 后恢复为收起态，没有把拖动位置残留到其他页面。
- [x] 层级守卫通过：星球层 `zoom-space` 和宠物日常层 `zoom-pet` 中任务面板均保持不可见，只在 Field 层显示。
- [x] 修正新手任务面板缩放源码契约：断言改为匹配当前 `panel.offsetHeight`、`stageWidth` 和 `stageHeight` 实现，未修改已通过三档真实 UI 回归的运行时代码。
- [x] 完成三轮真实失败远征：分别推进至第一章 `11 / 15`、`13 / 15` 和 `14 / 15`；第三轮进入最终首领 `PHASE 3 / 3` 并将其压至 `8 / 362 HP`，失败结果均真实回传远征记录，未带回战利品且未推进成功远征与周目标。
- [x] 第四轮通过事件、营地、商站与普通战斗完成针对性构筑，最终达到最大生命 `173`、累计额外基础攻击至少 `23`、额外护甲至少 `13`，并掌握“破甲震荡”。
- [x] 第四轮首次真实完成第一章：最终首领三阶段均通过公开战斗按钮击破，系统进入“第一章完成 · 中场营地”，自动获得 `90` 星币并开放第二章；确认第一章完成本身不构成可带回结算。
- [x] 确认中场“完整休整”在第二章首战真实生效，伙伴恢复为 `173 / 173 HP`、`44 / 44 MP`，护盾与冷却重置；事件获得攻击 `+8`，战术残页成功解锁“电弧弹跳”。
- [x] 第四轮最终在第二章第 4 层中断：将“曜曜月铃”压至 `82 / 449 HP`、捕捉率提升至 `18%` 后连续投掷三枚胶囊均失败，伙伴因敌方连续行动降至 `0 HP`；失败结算显示推进 3 个第二章节点，捕捉伙伴与战利品均未带回营地。
- [x] 累计完成六次真实捕捉尝试：第一章三次 `21%` 与第二章三次 `18%` 均失败；验证胶囊投掷会消耗整个回合，失败后敌方正常行动，低血量连续投掷风险很高。
- [x] 记录远征行为：商站每次只能购买一件；地图需点击当前 `.map-node.available`；首领包含三个阶段，阶段转换会中止该回合敌方攻击；首领不会固定在开战时补满玩家 HP。
- [x] 排除长流程服务干扰：Vite 开发服务的 HMR 曾两次中断远征；`vite preview` 因缺少源码绝对路径 `/js/expedition_capture_core.js` 无法运行完整链路。改用 `http-server` 在 `127.0.0.1:5173` 直接托管源码目录后，远征 iframe 可在不触发 HMR 的情况下连续操作。
- [x] 第六轮因自动验收脚本错误地将敌方意图解析为 `0`，在第一章 `13 / 15` 失败；改为读取公开元素 `#enemyIntent` 后，已能稳定识别 `26`、`47`、`52` 和 `56` 点伤害意图。
- [x] 第七、八轮均在第一章 `7 / 15` 失败。第八轮已正确选择“烤一串休息”、生命储备和战地急救包，并在高伤回合使用治疗与护盾；但连续 `56` 点蓄能冲击仍耗尽恢复、护盾和手雷，证明单纯延长防守回合不足以完成远征。
- [x] 核对敌方意图规则：显示“能量蓄积 · 下回合 N”时敌人当回合不造成伤害，下一回合才切换为“蓄能冲击 N”。第八轮脚本将两者都当作即时伤害，浪费了蓄力空窗；第九轮已按意图名称区分空窗和实际攻击，并完成首场战斗。
- [x] 第九轮在第一章首战后的强化选择处遭遇整页应用重载，远征 iframe 消失并返回登录入口。通过公开“游客体验”重新进入后得到新的 `Offline` 游客档案；公开菜单与档案页没有切换回原“柚柚冷雾”档案的入口。为遵守不修改存档约束，停止继续远征，未通过 localStorage 恢复或替换身份。
- [x] 修复同标签页游客身份刷新恢复：新增 `guest_session.js` 管理 `sessionStorage` 会话标记，启动时在无账号情况下恢复原游客档案，并在登录、退出和失败路径清除标记；同时将强制视图提前返回前的游客标记写入补齐。
- [x] 通过公开 UI 验证游客刷新恢复闭环，原伙伴仍为 `pet_fba0fykb / 柚柚冷雾`；未成年匿名显示“幼崽 #AAAA”属于既有命名规则，不是档案丢失。
- [x] 通过公开 UI 完成洗澡、喂食和拖放饲料，合法恢复伙伴状态后重新启航；全过程未修改存档、年龄、体力或战斗数值。
- [x] 定位远征周期性中断为测试环境外部主文档 `reload`：`performance` 导航记录为 `reload`，`pagehide` 发生时页面仍为 `visible`；源码与消息跟踪均确认 `gameStarted`、远征宿主和小游戏退出逻辑不会触发整页导航。
- [x] 在当前 Playwright 标签页临时拦截 `MagicHaqi.html` 主文档导航请求，两次请求均以 `ERR_ABORTED` 被阻止；该防护不落盘、不修改业务代码或玩家状态，证明后续失败来自真实战斗而非页面重载。
- [x] 真实 UI 首次成功捕捉 SR“糖糖陨龟”，但该局在第一章第 `14 / 15` 层前被外部重载中断，因此捕捉结果未完成结算，不能作为图鉴持久化通过依据。
- [x] 导航防护下的新一轮远征推进至第一章首领第三阶段：完成 14 个普通节点、4 场普通战斗和 1 场精英战斗；最终构筑达到 `224` 最大 HP，首领第三阶段剩余 `64 / 362 HP` 时被 `133` 点蓄能冲击击倒。失败结果正常回传且未带回战利品，确认当前单幼崽、战力 `61` 配置仍无法可靠完成两章结算。
- [ ] 两章完整成功远征、最终成功结算、捕捉结果持久化、图鉴解锁与再次孵化仍待具备可靠首领通关能力后继续验证。
- [ ] 材料、收藏、家园珍宝和矿区的成功结算链路仍待完整两章远征后验证。
- [ ] 成年伙伴小镇活动真实链路受伙伴年龄条件阻塞；当前真实档案只有未成年伙伴，不修改年龄数据绕过准入。

## 今日验证

- [x] 新手任务面板 Field 舞台等比缩放定向 Node 测试通过。
- [x] MagicHaqi 全量 Node 测试 `299 / 299` 通过。
- [x] 清除终端残留的 `MINIGAME_IDS` 筛选后，小游戏目录 Playwright 完整冒烟测试 `57 / 57` 通过。
- [x] 游客会话与离线档案恢复聚焦测试 `4 / 4` 通过。
- [x] 加入游客会话恢复覆盖后，MagicHaqi 全量 Node 测试 `304 / 304` 通过。

## 中午功能实现：HelloLearner 接入

- [x] 在保持 `webgames/tools/HelloLearner/` 零修改的前提下，完成 MagicHaqi 对 HelloLearner 的只读接入；工作区状态核对确认该目录没有文件变化。
- [x] 哈奇岛 Field 首页增加“学英语”快捷入口，与“星球探险 / 矿区”组成学习与探索入口组；入口沿用哈奇星球能力门禁，不影响其他星球。
- [x] 支持从首页入口和配置了 `helloLearner` 的导师 NPC 打开 HelloLearner，新窗口携带当前登录 token、语言和 `workspace=HelloLearner`。
- [x] MagicHaqi 使用 KeepworkSDK `PersonalPageStore.withWorkspace('HelloLearner')` 只读 `.hellolearner/progress.json`，不改动 HelloLearner 的写入协议或运行逻辑。
- [x] 用户从 HelloLearner 返回 MagicHaqi 后，通过窗口焦点恢复检查本次启动之后新完成的课程或角色扮演记录；完成记录必须带有效完成时间，旧记录不追溯奖励。
- [x] 新增奖励合同：每个自然日最多奖励一次，首次有效完成奖励 `20` 金币；完成记录使用“类型 + ID + 完成时间”去重，重复返回、重复聚焦或重复结算不会重复发奖。
- [x] 奖励结果写入 MagicHaqi 用户设置并触发云端保存，同时记录打开与发奖产品事件；远端进度读取失败仅给出警告，不阻塞主页面。
- [x] 新增 `hello_learner_rewards.js` 与聚焦测试，覆盖课程完成、角色扮演完成、无效记录、旧记录、重复完成和每日上限。

## 界面与手势调整

- [x] 调整 Field 首页快捷入口的布局与视觉权重，使“星球探险 / 矿区 / 学英语”成为稳定的场景级入口，保留任务面板和已有场景操作区。
- [x] 优化“今日一起做”横幅的美术框定位、标题层级、任务文本与操作热区；饥饿、清洁、心情、羁绊、远征和完成态分别使用匹配的框体参数。
- [x] 横幅只在 Field 层展示，装饰模式下不接收交互；手机、Pad 和桌面继续限制在舞台安全区内，避免横向溢出和被 Dock 遮挡。
- [x] 重写横幅拖动坐标：按下时记录指针相对横幅左上角的抓取偏移，移动时按抓取点计算目标位置，不再出现一拖就跳位。
- [x] 拖动位置改为保存客户端坐标并在重绘时恢复；窗口或舞台尺寸变化后重新夹紧到可见区域，避免横幅在渲染、缩放或切换后消失。
- [x] 为横幅与操作按钮设置 `touch-action: none`，统一 Pointer Event 拖动；记录最近拖动时间，拖动后的释放不会误触任务按钮，同时保留正常轻点跳转。
- [x] `home_daily_mission.test.mjs` 聚焦测试扩充至 `8 / 8`，覆盖抓取偏移、边界夹紧、拖动后点击抑制、Pointer Capture 清理和触摸策略。

## 启动故障排查与修复

- [x] 修复顶层 `js/app.js` 首次动态导入失败后永久停留蓝色 Loading：启动壳加入 `250ms / 500ms / 1000ms` 有限退避重试，重试 URL 添加 `boot_retry` 参数避开浏览器对失败 ES Module 的缓存。
- [x] 顶层模块连续失败后不再无限 Loading，改为显示“页面加载中断”和“重新加载”恢复操作；故障注入验证首次 `app.js` 请求失败后可由下一次自动重试恢复。
- [x] 复现并修复失效登录 token 的刷新循环：`/users/profile` 返回 `401` 后不再调用未等待的异步 `sdk.logout()`，改用 `sdk.setToken(null)` 同步清除 SDK token Cookie、用户缓存和认证状态。
- [x] 失效 token 故障注入验证通过：Cookie 被清空、页面无未处理 Promise rejection、Loading 消失并进入登录页；再次刷新不再请求 `/users/profile`。
- [x] 为本地 SDK 动态导入保留 `2s` 有界超时，防止 Edge 网络切换时 `import()` Promise 长时间悬挂。
- [x] 最终调整本地 SDK 策略：普通 `127.0.0.1:5501` 和 Vite 开发地址默认直接加载 CDN KeepworkSDK，不再无条件请求未启动的 `127.0.0.1:5001/index.ts`，消除控制台红色连接错误和额外等待。
- [x] 本地 SDK 联调能力改为显式启用：URL 添加 `?local_sdk=1`，或在 SDK 初始化前设置 `window.__useLocalKeepworkSDK = true`；仅此时访问 `5001`，失败或超时仍回退 CDN。
- [x] 区分应用 Loading 与浏览器导航挂起：一次旧 Edge + DevTools 标签连主 HTML 的 `DOMContentLoaded` 都无法到达，但 PowerShell 对相同 `5501` URL 返回 `200` 且新标签立即打开，确认该次停顿属于旧标签网络会话异常，不是 MagicHaqi bootstrap 死循环。
- [x] 新开 `5501` 标签验证最终策略：页面不产生任何 `5001` 资源请求，CDN SDK 正常就绪，Loading 消失并进入登录页；`5175` 游客页可正常恢复到“Offline的星球”。

## 验证与环境说明

- [x] `app.js` 编辑器诊断为零。
- [x] MagicHaqi 生产构建通过：Vite `5.4.21` 完成 `9` 个模块转换并正常生成 `dist` 产物。
- [x] 顶层模块首次失败恢复、失效 token 清理、无 token 再刷新和 CDN SDK 直载均完成浏览器故障注入或真实页面验证。
- [x] HelloLearner 目录通过 `git diff --numstat -- webgames/tools/HelloLearner` 核对为零修改。
- [x] 启动 `127.0.0.1:5173` 测试宿主后，小游戏目录全量 Playwright 冒烟测试再次达到 `57 / 57`；全部 iframe 均完成加载、可见内容检查且无本地请求失败或页面脚本错误。
- [x] 主页面在桌面 `1440×900`、Pad `1024×768`、手机 `390×844` 三种视口完成启动验收：登录和游客入口可见、无残留 Loading、无横向溢出、无页面脚本错误。
- [x] 最终 MagicHaqi 全量 Node 测试 `304 / 304` 通过；新手任务面板的源码合同测试已同步验证介绍卡片缩放上限与首页快捷入口避让逻辑。

## 今日结论与后续建议

## 美术资源补漏

- [x] 根据桌面截图复查今日主页美术，定位欢迎横幅仍通过 `new URL('../assets/qq.png', import.meta.url)` 引用被 Git 忽略的本地临时文件；部署后该文件不存在，浏览器会显示破图及底图缺失后的异常视觉碎片。
- [x] 将 `qq.png` 上传至 `https://cdn.keepwork.com/keepwork/cdn/magichaqi/assets/qq.png`，并把主页欢迎横幅改为稳定 CDN 引用；线上图片返回 `200`，尺寸为 `2259 x 818`，与 `.home-haqi-identity` 的宽高比一致。
- [x] 对欢迎横幅、三枚 `v3` 快捷入口、onboarding 路线图和六张每日任务框执行本地/线上 SHA-256 对账，全部内容一致；未发现第二个漏替换、旧版同名覆盖或仍指向本地 `assets/` 的生产资源。
- [x] 扩充主页资源源码契约测试，要求欢迎横幅使用 MagicHaqi CDN，并禁止恢复为 `../assets/qq.png` 本地引用。
- [x] 根据实机截图放大主页欢迎横幅：桌面宽度上限由 `340px` 提高至 `520px`，手机上限由 `390px` 提高至 `460px`；快捷入口动态对齐不再把横幅压回小于 CSS 首选宽度。
- [x] 将欢迎文案安全区调整为 `left: 17% / width: 66%`，避开左上城堡装饰和右侧关闭按钮；使用长宠物名在桌面、Pad、手机三档夹具复测，标题均未截断，文案未越界或与关闭按钮重叠。
- [x] 增加欢迎横幅尺寸与安全区源码契约，防止后续样式或动态宽度逻辑回退。

- [x] HelloLearner 奖励接入已从“待评审”推进为已实现的最小只读合同；当前边界是 MagicHaqi 读取既有完成记录并发放本地游戏金币，不修改 HelloLearner，也不反向写入其进度文件。
- [ ] 后续为 HelloLearner 奖励补做真实账号云端联调：分别完成一节课程和一次角色扮演，核对跨窗口返回、当日只发一次、次日重置，以及多设备登录后的用户设置同步。
- [ ] 评审 HelloLearner 长期奖励经济：当前 `20` 金币、每日一次适合作为首版防刷上限；扩大到连续学习、周目标或道具奖励前，应先核算金币产出占比并增加服务端可信校验，不能只依赖客户端时间和个人页文件。
- [ ] 将启动关键阶段增加结构化耗时记录，例如 `sdk-ready / auth-settled / profile-loaded / pets-loaded / first-render`；若线上再次出现 Loading，可直接判断停在哪个阶段，避免仅凭控制台截图猜测。
- [ ] 对 `loadCurrentUser()`、`loadUserProfile()`、`loadAllPets()` 等启动关键网络步骤评估统一超时和降级策略；网络切换或接口长时间不返回时，应保证最终进入登录页、游客模式或可重试错误页。
- [ ] 本地开发默认继续使用 CDN SDK；只有实际修改 KeepworkSDK 时才启用 `local_sdk=1` 并同时启动 `5001` 服务，避免把预期的本地端口缺失误判为业务错误。
- [ ] Edge DevTools 设备模拟或网络面板出现 `ERR_NETWORK_CHANGED`、`ERR_NETWORK_IO_SUSPENDED`，且页面标题显示浏览器级 `Loading http://...` 时，优先新开标签或重启 DevTools 网络会话；此状态下主 HTML 尚未返回，前端代码无法自我恢复。
- [ ] 为 `5501` 侧边栏入口增加轻量健康检查或一键重启 Live Server 的开发说明；正式验收长流程仍优先使用不触发 HMR 的稳定静态宿主。
- [ ] 两章成功远征、捕捉结算持久化、再次孵化、成年伙伴小镇活动和完整成果落点仍按上午记录继续待办，不以本轮入口、奖励或启动修复替代真实 UI 验收。