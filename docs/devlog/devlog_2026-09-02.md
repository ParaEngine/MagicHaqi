# 开发日志：2026-09-02

## 今日范围

- 今天是 15～20 个工作日邀请测试包计划的第 6 天，继续执行第 5～8 天“串联伙伴、小镇、远征、矿区和结算反馈”的阶段任务。
- 本轮聚焦三处高曝光视觉：宠物图鉴、玩耍大厅和哈奇星球主界面 Dock，并完成主界面右上 HUD 美术替换；不修改支付、经济、存档和场景跳转业务。
- 第 6 天继续沿真实玩家路径收口已有功能，不新增地图、伙伴或玩法；两次两章远征和成年伙伴小镇链路仍只接受真实 UI 证据，不注入存档或伪造结算。

## 宠物图鉴美术收口

- [x] 图鉴页面接入星图场景背景，保留天空、云层和草地等完整环境，不再改动整张背景来处理局部装饰。
- [x] 清理页面中不需要的树木、花丛、石头和木桩装饰引用，避免滚动时出现半截素材；标题、数量、状态和按钮文案继续由 DOM 动态渲染。
- [x] 从美术源图切出并接入普通、选中两套分类页签资源，保留原 tab ID、点击逻辑、选中态和无障碍语义。
- [x] 分析 `assets/yj.png` 的 alpha 内容边界：原图为 `1746×186`，有效内容位于 `(22, 26, 1704, 146)`；四周保留 `6px` 安全边距后按 `(16, 20, 1732, 178)` 裁切，透明 WebP 最终有效尺寸为 `1716×158`。
- [x] 研究放归按钮使用 Pillow `quality=90`、`method=6`、`exact=True` 压缩，输出文件为 `33,798 bytes`；上传至 `keepwork/cdn/magichaqi/assets/ui/pet-catalog/pet-catalog-research-release-v5.webp`，远端返回 HTTP 200、`image/webp`，浏览器解码尺寸与本地一致。
- [x] 页面按素材自然比例 `1716 / 158` 接入研究放归按钮，移除移动端会拉高按钮的最小高度和垂直内边距；删除与素材烘焙文字重复的可见 DOM 文案，仅保留 `aria-label="一键研究放归重复 N / R 伙伴"`。
- [x] 修复稀有宠物页签选中态的数量对比度：数量由白色改为深蓝 `#24577d`，避免与浅色按钮底图混在一起。
- [x] 图鉴标题脱离左右不等宽控件的 flex 分配，改为相对整个顶栏绝对几何居中；桌面、Pad、手机三档实测标题中心误差均为 `0px`。
- [x] 10 个稀有宠物分类由单行横向滚动改为 5 列 2 行自适应网格。三档视口下 10 项均完整位于容器内，按钮内容溢出、分类区滚动溢出和页面横向溢出均为 `0`。
- [x] 修复页签素材串用：带“我的宠物”烘焙文字的资源固定给“我的宠物”，带“稀有宠物”烘焙文字的资源固定给“稀有宠物”，选中状态只改变光效，不再替换另一张带文字素材。
- [x] “我的宠物”数量在选中和未选中时统一保持白色，选中时增加青蓝外发光区分状态；“稀有宠物”继续使用金色光效和深蓝数量，两个页签的状态语义及点击逻辑不变。
- [x] 更新图鉴契约测试，锁定星图背景、类型固定页签资源、研究放归 v5 资源、空按钮体、标题几何居中、五列分类网格、计数颜色和选中光效。

## 玩耍大厅视觉优化

- [x] 49 个小游戏卡片统一使用确认完整的卡框资源，撤销把场景块误当完整卡框的错误轮换；卡片采用固定比例布局，动态标题、奖励、时长和状态不再撑开或覆盖卡片。
- [x] 从 `ws.png` 提取首页、探索、创造和我的四个底栏图标，重建为完整圆形徽章并接入 v2 CDN 资源；随后按实页反馈放大桌面和手机图标，调整底栏高度与正文安全区。
- [x] 放大玩耍标题牌和标题文字，桌面、Pad、手机断点分别约束尺寸；移除“哈奇小镇活动”和“今日精选 4 款”两处累赘提示框，让游戏列表直接承接标题区。
- [x] 修正网页版试玩顶栏的标题对齐：标题牌脱离左右按钮的 flex 宽度分配，固定以小游戏 iframe 的水平中线居中，收藏和分享按钮不再把标题推向左侧。
- [x] 卡片网格固定为桌面 5/7 列、Pad 3 列、手机 2 列，窄屏下无卡片相交、文字覆盖和横向溢出。

## 哈奇星球主界面 Dock

- [x] 左侧建造、清理、玩耍、睡觉和孵化仓五个操作按钮保留本轮已确认的新资源、尺寸、位置与业务逻辑，后续调整不再触碰该区域。
- [x] “今日一起做”横幅的按钮文案统一限制为三个汉字：去进食、去洗澡、去玩耍、去陪伴、看星图和看记录；体力、清洁、心情、陪伴、探险和完成六张底图按各自美术按钮区域独立配置文字盒位置与尺寸。
- [x] 从 `assets/crz.png` 按小镇广场、阳光海滩、魔法密林、游乐场、雪山、魔法学院、跳跳农场顺序切出七张场景按钮。
- [x] 七张切图按 alpha 内容紧切并保留 6px 安全边距，压缩为透明 WebP，上传至 MagicHaqi CDN 并以全新 `*-crz-v4.webp` 文件名逐项替换。
- [x] 新图片自身作为完整按钮显示，保持自然宽高比；删除旧紫色按钮的背景、边框、光圈和阴影，选中态不再把新资源镶嵌进旧外壳。
- [x] 修正哈奇星球判定：通过 `?home_planet=haqi` 进入且存档尚未恢复时，七场景按钮仍使用哈奇专属资源。
- [x] 保持桌面原有左右排版；手机端五个操作按钮按可用空间等比缩放，Pad 横屏右侧场景区在自身边界内横向滚动，不再覆盖左侧按钮。

## 哈奇星球右上 HUD

- [x] 从 `assets/zm.png` 按透明连通区域切出心情、生物燃料、金币、菜单和音乐五张完整图片，保留透明安全边距并压缩为 WebP，五张合计约 51 KB。
- [x] 五张资源上传至 `keepwork/cdn/magichaqi/assets/ui/home-hud/`，页面全部改用对应 CDN 地址。
- [x] 心情、生物燃料和金币继续显示实时数值；菜单继续沿用 `#mhMenuBtn` 的点击和右键入口；音乐继续沿用 `[data-field-music-toggle]`，并同步静音样式、标题和 `aria-pressed` 状态。
- [x] 新增 HUD 美术回归测试，锁定五张资源、实时数值属性和菜单/音乐交互钩子。
- [x] 根据实页反馈二次修复旧切片残留的低透明度正方形底和菜单破图：清理外围低透明矩形层，按主体重新紧裁切，输出并接入五张 `*-v2.webp`，以新对象名绕开旧 CDN 缓存。
- [x] `v2`、`v3` 实页复核仍能看到正方形边缘；最终将几何蒙版内缩并增加 4px 全透明安全边距，输出并接入五张 `*-v4.webp`。五张图片四条最外边 alpha 均严格为 0，白底、黑底和棋盘格 5 倍放大检查无矩形残边，同时保留按钮描边、阴影和底部数字区域。

## 运行稳定性修复

- [x] 星际矿区宿主对相同状态快照增加幂等过滤；未发生业务变化时不再重复保存或触发首页整页重渲染，避免连续同步造成卡顿，同时保留经济数据与防重放账本的宿主所有权。
- [x] 星球探险恢复旧存档时记录已确认缺失的伙伴档案；同一缺失伙伴只尝试补载一次，避免“补载失败 → 重渲染 → 再次补载”的 Promise 微任务循环，成功补载的伙伴不会被误记为缺失。

## 验证结果

- [x] 七张 `crz-v4` CDN 资源均完成 HTTP、内容类型和浏览器解码验证，无破图或旧缓存污染。
- [x] 主界面按实际 CSS 视口覆盖 `320/390/768/820/1024/1440px` 六档测试，操作按钮和七场景按钮无裁切、拉伸、重叠或非预期整页横向溢出。
- [x] 试玩标题居中契约测试通过；桌面浏览器按真实顶栏结构测得标题牌与 iframe 水平中心误差小于 `0.001px`，且未遮挡返回、收藏或分享按钮。
- [x] “今日一起做”六种按钮状态的三字文案与逐图定位契约测试通过，运行时代码中无遗留“去补充体力”等超长 CTA；浏览器按底图原始尺寸核对六种文字盒中心，最大误差约 `1.9px`。
- [x] 最终桌面实页由用户确认：七场景按钮大小、比例和无紫色外壳状态正确，保持该状态不再调整。
- [x] 首页右上五张 `v4` CDN 图片均完成下载 SHA-256 一致性及真实页面解码验证；自然尺寸依次为 `102×104`、`102×104`、`102×104`、`141×135`、`123×127`。真实页面截图确认透明轮廓无正方形残边、数字牌和阴影未被裁切；菜单可正常打开，音乐 `aria-pressed` 可在点击后切换。
- [x] 宠物图鉴定向测试 `3/3` 通过；研究放归按钮在移动端保持 `1716:158` 自然比例且无水平溢出，稀有页签选中数量的计算颜色为 `rgb(36, 87, 125)`。
- [x] 宠物图鉴页签在选中和未选中两种状态下逐项核对背景 URL：“我的宠物”始终使用自身资源，“稀有宠物”始终使用自身资源；“我的宠物”数量两种状态均为 `rgb(255, 255, 255)`，选中态青蓝光效正常生效。
- [x] MagicHaqi 全量 Node 自动化 `275/275` 通过，相关文件编辑器诊断无错误；按项目约束未执行 build。
- [x] 小游戏目录 Playwright 冒烟测试 `57/57` 通过；首页使用真实 viewport 覆盖 `320×568`、`390×844`、`768×1024`、`820×1180`、`1024×768`、`1440×900`，六档均无整页横向溢出，五张 HUD 均完成解码且位于视口内。
- [x] 发布前审计确认运行时新美术均位于 `assets/*` 忽略范围并通过 CDN 引用，未进入 Git 暂存区；旧版七张地点按钮的本地删除也从本次非美术提交中排除，不改动用户本地美术状态。

## 第 6 天阶段进度

- [x] 伙伴：宠物图鉴、伙伴照料入口、远征准备状态提示和成果展示均已形成可用串联；图鉴本轮完成三端视觉收口。
- [x] 小镇：哈奇星球入口、玩耍大厅、小游戏金币收益和返回路径已通过三端检查；未成年伙伴的准入说明保持真实规则。
- [x] 远征：真实 UI 已取得喂食、准备、第一章首节点、奖励、退出和历史记录回流证据；远征历史只在主动展开时记录一次匿名查看事件。
- [x] 矿区：真实完成第 1～4 层、`4/4` 货舱封装、主动撤离和 4 个原石回写；手机和 Pad 的画布、目标区、机械爪、矿石区及底部磁锁均完整可见。
- [x] 结算反馈：远征成果弹层、四类成果落点、分享卡 `1080×1350` PNG 导出和一次性奖励幂等条件均已验证。
- [x] 免费邀请版支付边界采用“默认关闭、显式开启”：只有发行环境设置 `globalThis.MagicHaqiReleaseConfig.realPaymentEnabled === true` 才允许进入真实支付，缺失配置、字符串或数值均不能误开启。
- [x] 支付关闭时，小游戏 `haqi_open_vip` 只核验并回传现有会员状态，不打开支付页；广告解锁走宿主降级流程，不调用可能展示购买入口的 SDK `requestUnlock()`，弹层只保留“看广告解锁”和“取消”。
- [x] 支付最终入口增加第二道门禁，SDK `openVipMembership()` 与旧版 `/p/vb/vipPayOrder` URL 均无法绕过；已核验会员仍可保留免广告权益。商业状态与小游戏宿主门禁专项测试 `8/8` 通过。
- [ ] 两次两章远征真实 UI 验收仍未完成。当前集成浏览器会把 iframe 内可见按钮判为不可见，不能在不强制 DOM 点击的前提下可靠跑完两次、每次两章共 30 层；保留到稳定可见浏览器会话继续完成。
- [ ] 成年伙伴“小镇活动 → 奖励 → 伙伴照料/冒险准备”真实链路仍缺具备成年伙伴的测试账号；不通过修改年龄或注入存档绕过准入。

## 接下来

- [x] 宠物图鉴本轮视觉收口已完成，不再继续无边界微调。
- [ ] 保持已确认的主界面七场景按钮和左侧五操作按钮不变，避免美术优化扩散到已验收区域。
- [ ] 第 6～8 天优先补齐两次两章远征和成年伙伴小镇链路的真实 UI 证据；环境或账号条件不具备时继续保留明确阻塞，不用自动化契约冒充真实试玩。
- [ ] 商业发行如需恢复真实支付，必须由受控发行入口显式注入布尔配置，并补做登录、游客、小游戏会员入口和权益恢复回归；免费邀请版保持默认关闭。