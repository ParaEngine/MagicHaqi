# 开发日志：2026-08-21

## 当前阶段

今天承接 2026-08-20 尚未完成的小游戏日常编排、完成协议统一和 NPC 深层互动。工作继续遵循“先做可单点验证的具体体验，再扩大范围”的节奏，不提前进入充值接入或外部试玩准备。

## 今日目标

- 将每日精选从纯稳定轮换升级为带正式分类、预计时长和成长方向的策划组合，目标为 4 款、约 10 分钟。
- 为小游戏完成消息建立统一判定，区分成功、失败、主动退出与无效字段，避免只要发送结束消息就结算奖励。
- 在农场或动物园落地首个“拿起物品并送到目标角色”的携物任务，作为 NPC 玩法从纯点击目标向移动协作过渡的样板。
- 为以上改动补充自动化测试，并完成桌面端与移动端实测。

## 资源同步

- 从 `origin/master` 拉取最新资源，本地由 `87b011df8` 快进到 `ed361f57b`。
- 远端新增变化仅涉及 `dist-games/housework-master/index.html`，与 MagicHaqi 当前开发范围无冲突。

## 今日完成

- 已完成远端资源同步并确认工作树基线。
- 已定位每日精选的控制路径：`js/minigame_daily.js` 负责稳定轮换与奖励去重，`js/view_minigames.js` 负责大厅展示、完成消息接收与实际结算。
- 新增动作、益智、策略、照料四类小游戏策划档案，从已校准候选池每天各选一款，并优先选择总时长最接近 10 分钟的组合；同日稳定、次日轮换。
- 大厅精选区展示当日总预计时长、分类覆盖、单款成长方向和预计分钟数。
- 新增主机级完成消息归一逻辑：明确失败、退出和取消不结算，明确成功正常结算；缺少状态字段的旧游戏暂以 `legacy-finished` 兼容并保留可观测原因。
- 将普通 iframe 与 Explore 两条完成路径统一接入归一逻辑，失败完成不再播放奖励音效。
- 将跳跳农场咕噜大叔原有顺序灌溉任务升级为首个专属携物任务：先拿起种子篮，携带到播种台，再共同确认田垄。
- 场景任务配置新增 `interactionMode: carry` 和 `pickup / destination / confirm` 步骤角色；携物任务按阶段开放目标，种子篮会显示在主宠物上方，交付前可点按放下后重新拿取。
- 携物状态沿用现有 `sceneCollected`、`activeSceneInteractionId` 和完成标记存档，旧任务及旧存档无需迁移。
- 使用已解锁哈奇岛的同源游客存档完成咕噜大叔携物任务实测：普通点按拿取、点按携带物取消、重新拿取、交付、最终确认、完成标记回访均正常；三步进度和完成标记在重新进入农场后仍保留。
- 实测发现主宠物 SVG 会在播种台附近截获点击，将场景任务目标层提升到主宠物之上；同时移除目标位移动画，并将携带物无障碍名称修正为“放下种子篮”。
- 为同场景第二位 NPC 莫卡新增“巡查新播田垄”连续任务，要求先完成咕噜大叔的播种任务，再依次核对土壤标牌、测量第一株新芽和标记运输路线。
- 新增 NPC 互动关系奖励结算：互动首次完成增加配置心意，重复完成不重复结算；跨关系阶段时发放配置收藏品并记录已领取阶段。莫卡任务提供心意 +6，跨至“熟悉”时奖励向阳小花。
- 使用已解锁哈奇岛的同源游客存档完成莫卡连续任务实测：前置任务完成后入口正常解锁，三处场景目标可依次点按，完成后入口变为调查结论，关系从“初识 0/20”增加至“初识 6/20”。本次未自然跨阶段，因此未伪造向阳小花发放；自动化测试已覆盖 16→22 的跨阶段奖励。
- 莫卡任务回访只展示调查结论，不重复增加心意或收藏品；刷新并重新进入游客存档后，“查看新苗巡查记录”完成标记和 6/20 关系值均保留。
- 开始逐款迁移小游戏显式完成状态：`webgames/data/mahjong.html` 在唯一终局入口发送 `gameFinished`，玩家胡牌明确为成功，电脑胡牌与流局明确为失败，并携带 `outcome`、积分和难度。
- 第二款迁移为 `minigames/haqi_gomoku.html`：统一 `finish()` 终局入口新增 `status`、`completed`、`success` 和 `outcome`，玩家获胜明确成功，和棋与宠物获胜明确失败，避免宿主旧兼容分支误发奖励。
- 将莫卡后续任务扩展为“协作运送第一车果实”：完成新苗巡查后，依次装好分层果篮、携带并沿标记路线入库、与咕噜大叔共同验收。任务开始和每个节点推进时，莫卡与咕噜大叔按配置偏移同步走位。
- 修复携带物嵌在可拖动主宠物内时只收到 `pointerdown` 的问题：场景平移和主宠物拖动均忽略 `.field-scene-carry-item`，移动端尺寸下可普通点按放下并重新拿取。
- 第三款迁移为 `minigames/haqi_xiangqi.html`：唯一 `finish()` 终局入口新增 `status`、`completed`、`success` 和 `outcome`，玩家执红获胜明确成功，和棋与宠物获胜明确失败。
- 紧急修复新号新手航线面板覆盖小游戏大厅的问题：全局 `#mhOnboardingPanel` 原本只在登录页隐藏，进入 `minigames` 后仍以固定定位和高层级拦截卡片；现将视图可见性提取为可测试规则，并在登录页和小游戏视图隐藏面板、清理引导高亮，返回家园后正常恢复。
- 完成 NPC 场景任务后不再将“巡查记录”等完成标记永久贴在广场或农场背景上；完成状态、奖励和调查结论继续保留，回访统一从 NPC 对话进入。
- 普通场景任务的步骤提示改为依据实际完成数量生成，不再把配置数组中的“第一处/最后一处”文案当作固定点击顺序；玩家先点第三个目标时会正确显示该目标已处理、进度 1/3、还剩 2 处。显式 `interactionMode: carry` 的拿取、交付任务仍保留顺序锁定和原步骤反馈。
- 修复广场背景仍显示大量任务步骤物件的问题：此前场景渲染会把尚未接取的 NPC 任务也画成不可操作目标，完成一项任务后其它未激活步骤仍留在背景。现在步骤物件仅在该任务为当前进行中时显示；未接取和已完成任务均不渲染，完成结算后立即移除。
- 紧急修复步骤可见性重构引入的字段页白屏：移除局部 `active` 变量后，携物步骤锁定表达式仍引用该变量，导致运行时出现 `active is not defined`。现直接依据携物状态计算锁定；刷新共享字段页后不再出现渲染错误，聚焦测试 8/8、全量测试 151/151 和 production build 均通过。
- 修复进行中任务的已完成步骤仍以“已固定/已亮起”卡片留在场景、与下一目标重叠的问题：带 `resolvedIcon` 的步骤此前使用可见的 `.is-resolved` 样式。现在已解决步骤与普通已收集步骤一致立即隐藏且不参与点按，同时保留 DOM，携物取消时移除完成类后仍可恢复拿取目标。
- 修复罗德镇长广场巡查完成后仍可再次接取、接取后无目标可点的卡死：部分存档已记录全部 `sceneCollected` 步骤，但缺少 `completedInteractionIds`，对话框因此误判为未完成，而场景又将三步全部视为已处理。现在完成判定同时识别正式完成 ID 和“配置步骤已全部收集”的事实；进入字段时自动补齐完成 ID、剧情解锁标记并清除误激活状态，且不重复发放关系或收藏奖励。
- 修复咕噜大叔“观察潮汐池”等场景任务首次接取后没有目标可点的问题：未接取任务按设计不会预先生成目标 DOM，但接取回调此前只尝试激活已有节点。现在写入活动任务后立即同步刷新字段，生成当前任务的三个目标并重新绑定点击事件，再继续执行携物锁定、NPC 走位和任务提示。

## 验收记录

- `node --test tests/minigame_daily.test.mjs`：5/5 通过。
- 三组聚焦测试合并执行：19/19 通过；其中场景任务 5/5，NPC 礼物与关系奖励 9/9，每日精选 5/5。
- VS Code 诊断：`config.js`、`level_field.js`、`npc_gifts.js`、每日精选相关模块、测试和 `mahjong.html` 均无错误。
- `npm run build`：Vite 5.4.21 构建成功，95 个模块完成转换；仅保留原有非 module 脚本、运行时 URL 和大包体警告。
- 浏览器桌面实测完整闭环咕噜大叔携物任务；持久化记录包含三个 `sceneCollected` 步骤、`completedInteractionIds: ["irrigate_field"]`，活动任务已清空。
- 集成浏览器平板级视口 `695×611` 下页面宽度与视口一致，无横向溢出；携物目标可普通点按，任务完成状态与回访内容均正确保留。
- 浏览器桌面实测莫卡任务的解锁、三步完成、结算、回访去重和刷新持久化；关系值自然增加 6，完成状态在重载后仍保留。
- 五子棋显式完成协议聚焦断言通过，VS Code HTML 诊断无错误。
- 浏览器 `695×611` 视口实测双角色运输完整闭环：中途携带状态刷新恢复后可普通点按取消，第一步恢复、后续步骤重新锁定；重新拿取后完成路线交付和共同验收，两位 NPC 在各节点同步移动。
- 运输结算将莫卡关系从 6/20 自然增加到 14/20，并发放配置收藏品“贝壳风铃”；完成后仅展示首车入库记录，重复回访不再次结算，刷新后记录保留且无活动步骤或残留携带物。
- 象棋显式完成协议聚焦断言通过，VS Code HTML 诊断无错误。
- 新号移动端 `390×844` 实测：家园首次进入正常显示新手航线；通过小游戏深链进入大厅后面板为隐藏状态，卡片中心命中实际卡片内容；启动宠物贪吃蛇后 iframe 顶部命中 iframe 而非全局面板；返回家园后新手航线恢复。
- 新号桌面端 `1440×900` 实测：小游戏大厅面板为隐藏状态，第一张今日精选卡片可正常命中，无全局新手层遮挡。
- 新手面板视图规则自动化回归及原有新手任务测试共 16/16 通过，相关 JavaScript 与测试文件无 VS Code 诊断错误。
- 场景任务聚焦测试 8/8 通过，覆盖普通任务先点第三目标、全部目标完成、携物任务保留配置反馈，以及未接取隐藏、进行中显示、已完成隐藏三种步骤可见性状态；代码与样式中已无旧完成标记引用。
- MagicHaqi 全量 Node 回归测试 151/151 通过；普通 production 构建成功，95 个模块完成转换，最终补丁空白检查与相关文件诊断均通过。
- 已完成步骤隐藏修复再次通过场景任务聚焦测试 8/8、全量 Node 测试 151/151、Vite production build（95 个模块）及 CSS 诊断；共享浏览器页当前均已回到登录状态，因此本轮未追加已登录任务现场的浏览器复测。
- 罗德镇长重复接取修复通过场景任务聚焦测试 10/10，新增覆盖半完成存档的完成识别、不可重接、正式状态补齐和重复迁移幂等性；MagicHaqi 全量 Node 测试 153/153、Vite production build（95 个模块）及相关 JavaScript/测试诊断均通过。共享浏览器页仍为登录状态，本轮未直接修改或伪造截图账号存档。
- 首次接取目标生成修复通过场景任务聚焦测试 11/11，新增约束“保存激活状态后先刷新字段、再查询新目标节点”的生命周期回归；MagicHaqi 全量 Node 测试 154/154、Vite upload 模式构建（95 个模块）及相关 JavaScript/测试诊断均通过。共享浏览器页仍为登录状态，因此尚未在截图账号现场复测。
- 修复字段页点击“建造”后出现 `itemName is not defined` 的渲染错误：建造托盘调用了 `itemName()`，但 `level_field.js` 未从 `i18n.js` 导入该函数。现已补齐导入，并增加源码契约测试防止调用与导入再次脱节；场景任务聚焦测试 12/12、MagicHaqi 全量 Node 测试 155/155、Vite upload 模式构建（95 个模块）及相关文件诊断均通过。
- 建造模式已完成双端浏览器闭环实测：桌面端 `1440×900` 与移动端 `390×844` 均可进入建造、显示本地化名称“一间小屋”、选中 `house_1` 并点击“完成”返回普通字段操作栏；全程无 `渲染错误` 或 `itemName is not defined`，文档宽度分别为 1440/1440 与 390/390，无横向溢出。本轮使用隔离游客会话，未修改截图账号存档。
- 调整玩耍大厅卡片密度：全部小游戏图标由 `58px / 48px` 缩至 `46px / 38px`，卡片统一采用“图标、最多两行标题、单行补充信息”的三段式布局。今日精选不再使用左上角标签或展示冗长成长说明，改为底部单行“精选 · 分类 · 分钟”，避免首排标签、图标和文字互相挤压。
- 玩耍大厅完成桌面端 `1538×768` 与移动端 `390×844` 浏览器实测：首排四张精选卡片及后续普通卡片均无子元素越界，页面宽度分别与 1538、390 视口一致，无横向溢出；每日精选聚焦测试 5/5、全量 Node 测试 155/155、Vite upload 模式构建（95 个模块）和脚本诊断均通过。
- 为莫卡新增宠物协作日常“和宠物完成农场巡田”：完成首车入库后，先点自己的宠物嗅闻样本，再跟随气味目标，最后与莫卡、咕噜大叔共同确认采摘区。三步严格顺序开放，宠物头顶显示“点我协助”，完成后发放心意 +10、收藏掉落并同步结算今日委托。
- 场景 NPC 委托改用“星球 + 字段 + NPC”作用域关系键进行当日去重和关系累计，避免不同场景中复用 `npc_2` 等原始 ID 时串用进度；现有小游戏 NPC 调用仍兼容原始 ID。桌面端与移动端 `390×844` 均完成真实三步闭环，移动端无横向溢出；运行时确认 `completedCount: 1`、心意和收集记录只写入 `haqi:7:npc_2`，不会生成裸 `npc_2` 关系数据。相关聚焦测试 20/20 通过，修改文件诊断均无错误。
- 第四款显式完成协议迁移为 `minigames/haqi_thunder.html`：结算消息统一携带 `status`、`completed`、`passed`、`success` 和 `outcome`，同时保留旧宿主使用的 `won` 字段；雷云聚焦协议测试 7/7 通过。
- 将魔法密林多克特博士的生态调查升级为第二个宠物协作场景：依次点击自己的宠物、调查异常足迹、与博士确认恢复标记，完成后发放心意 +8 和竹叶收藏品，并写入场景作用域剧情标记。移动端 `390×844` 完成真实闭环与重载持久化，关系数据仅写入 `haqi:3:doctor_dokter`。
- 修复“制作森林星灯”前置条件看似故障的引导问题：此前锁定按钮只显示任务名称，玩家容易把博士的今日小游戏误认为生态调查主线。所有带 `requires` 的互动现可配置 `unlockHint`，锁定按钮在点击前直接显示地点、NPC 和任务类型，点击后再展示完整条件；博士对话也明确区分“宠物生态调查”场景主线与“今日小游戏：反应训练”。
- 已为庆典演出、森林星灯及农场三段连续任务补齐路线提示；星灯入口明确显示“魔法密林 → 多克特博士 → 宠物生态调查”，详细说明明确今日小游戏不能代替前置场景任务。
- 跨场景引导聚焦测试 15/15 通过。真实青龙导师弹窗中，桌面与移动端 `390×844` 均在点击前完整显示星灯解锁路线；按钮实际尺寸与滚动尺寸一致，无文字裁切。点击后详细说明完整显示“今日小游戏不会解锁森林星灯”，移动端截图无控件遮挡。
- MagicHaqi 全量 Node 回归测试 160/160 通过，相关 JavaScript、CSS、JSON 和测试文件诊断均无错误，`git diff --check` 无空白错误。
- Vite upload 模式构建成功，95 个模块完成转换并生成稳定发布产物；仅保留既有非 module 脚本、运行时 URL 和大包体警告。
- 最终桌面端 `1440×900` 回归覆盖字段页、魔法学院青龙导师和魔法密林多克特博士：页面宽度与视口一致，无横向溢出或渲染错误；星灯锁定按钮在点击前显示完整路线，按钮约 `202×46` 且内容滚动尺寸未超过可视尺寸，点击后解锁说明完整可读。
- 最终移动端 `390×844` 回归完整走通博士宠物生态调查：接取后仅宠物步骤可操作，后续目标明确显示“尚未轮到”；依次完成宠物辨味、足迹调查和恢复标记确认后，场景目标立即清理，并正常结算心意 `+8`、今日委托和竹叶收藏品。页面全程保持 `390/390`，无横向溢出、文字裁切、按钮遮挡或误触。
- 最终持久化检查确认生态调查写入 `haqi:3:doctor_dokter` 作用域关系和剧情完成标记，未生成裸 `doctor_dokter` 关系键；任务完成后没有残留活动步骤，重新打开 NPC 对话进入回访状态，不会重复结算。
- 最终小游戏大厅双端回归覆盖移动端 `390×844` 与桌面端 `1440×900`：前 12 张卡片均无子元素越界，首张精选卡中心命中卡片自身；新手航线面板在大厅中高度为 0，不遮挡卡片或 iframe，页面无横向溢出。浏览器仅出现本地 KeepworkSDK `127.0.0.1:5001` 不可用后自动回退 CDN 的预期警告，未发现应用脚本异常。

## 仍未完成

- 继续将其余独立小游戏逐项迁移到显式成功/失败协议；当前主机仍为缺少状态字段的旧游戏保留兼容分支。
- 全产品完整度审计、充值接入验收和外部试玩准备。

## 下一步

继续按今日精选候选和本地可编辑源码清单迁移显式完成状态；同时审计新增长任务链，确保锁定入口都在点击前给出可执行的地点、角色和任务类型提示。
