{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build --config vite.config.mjs",
        "dev": "vite --config vite.config.mjs",
        "upload": "npm run build && node uploadRelease.mjs"
    },
    "devDependencies": {
        "vite": "^5.4.19"
    }
}